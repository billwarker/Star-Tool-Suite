CREATE TABLE merchant_sku
(vendor_sku VARCHAR(255) PRIMARY KEY NOT NULL,
walmart_sku VARCHAR(255),
walmart_man_sku VARCHAR(255),
bestbuy_sku VARCHAR(255),
source_sku VARCHAR(255),
staples_sku VARCHAR(255)
);
